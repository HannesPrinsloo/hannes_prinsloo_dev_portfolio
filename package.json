{
    "name": "hannes-prinsloo-portfolio",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "build": "npm run build --workspaces",
        "dev": "npm run dev --workspaces"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    },
    "dependencies": {
        "@vercel/analytics": "^1.6.1",
        "framer-motion": "^12.34.0",
        "lucide-react": "^0.564.0"
    }
}
